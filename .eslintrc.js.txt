// Deactivated file (with the .txt extension)

// See https://eslint.org/docs/latest/use/configure/configuration-files
// See https://craco.js.org/recipes/use-an-eslint-config-file/

module.exports = {
  extends: ['eslint-config-react-app'],
};

// NOTE: I haven't been able to get rid of this warning yet (for example, when
// compiling on a remote server):
// craco:  *** Cannot find ESLint loader (eslint-loader). ***
// I made some attempts to get rid of this with reference to the following.
// * https://www.npmjs.com/package/eslint-loader  (deprecated)
// * https://www.npmjs.com/package/craco-eslint-webpack-plugin  (latest)
//   Attempted to install but was unable to resolve dependency tree.
